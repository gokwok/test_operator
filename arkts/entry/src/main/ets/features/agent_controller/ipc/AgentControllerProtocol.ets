export const AGENT_CONTROLLER_DESCRIPTOR: string = 'com.huawei.aios.agent.operation.AgentController';

export enum AgentControllerRequestCode {
  PING = 1,
  GET_SNAPSHOT = 2,
  START_TASK = 3,
  STOP_TASK = 4,
  PULL_EVENTS = 5,
  SUBMIT_USER_REPLY = 6,
  START_TASK_TEXT = 7,
  ENSURE_VTS = 10,
  GET_RUNTIME = 11,
  LAUNCH_APP = 12,
  CLICK_NORM = 13,
  SCREENSHOT = 14,
  DESTROY_VTS = 15,
  STOP_VTS = 16,
  SCREENSHOT_CHUNK = 17,
  LONG_SCREENSHOT = 18,
  LONG_SCREENSHOT_CHUNK = 19,
  SET_VIEWER_ACTIVE = 20,
  GET_VIEWER_MODE = 21,
  SET_VIEWER_MODE = 22,
  ATTACH_VIEWER_SURFACE = 23,
  SEND_VIEWER_EVENT = 24,
  PULL_VIEWER_EFFECTS = 25,
  TRANSFER_TO_MAIN = 26,
  LAUNCH_APP_BY_NAME = 27,
  ACTION_DOUBLE_CLICK = 30,
  ACTION_LONG_CLICK = 31,
  ACTION_SWIPE = 32,
  ACTION_TYPE = 33,
  ACTION_PRESS_BACK = 34,
  ACTION_PRESS_ENTER = 35,
  ACTION_PRESS_BACKSPACE = 36,
  ACTION_PRESS_KEY = 37,
  ACTION_PRESS_COMBO_KEY = 38,
  ACTION_REQUEST_FOCUS = 39,
  SET_CLOUD_ENDPOINT = 40,
  DEBUG_CALL_USER = 41,
  DEBUG_INTERACT = 42,
  DEBUG_TAKE_OVER = 43,
  REQUEST_TAKE_OVER = 44,
  REQUEST_SUPPLEMENT = 45,
  SUBMIT_USER_NOTE = 46,
  RELEASE_AGENT_SPACE_IF_FINISHED = 47,
  TRANSFER_AND_RELEASE_IF_FINISHED = 48,
  CLOSE_LIVE_VIEW = 49,
  // Large payloads should NOT be returned in a single IPC response.
  // Use length + chunk requests to avoid IPC transaction size limits.
  GET_A11Y_TREE_LIST_LEN = 50,
  GET_A11Y_TREE_LIST_CHUNK = 51,
  DEBUG_SIMULATE_TOOL_CALL = 54,
  // Large payloads should NOT be returned in a single IPC response.
  // Use length + chunk requests to avoid IPC transaction size limits.
  GET_VISIBLE_PAGE_CONTENT_LEN = 55,
  GET_PAGE_CONTENT_LEN = 56,
  GET_VISIBLE_PAGE_CONTENT_CHUNK = 57,
  GET_PAGE_CONTENT_CHUNK = 58,
  MCP_CONNECT = 60,
  MCP_DISCONNECT = 61,
  MCP_STATUS = 62,
  MCP_LOGS = 63,
  MCP_CLEAR_LOGS = 64,
  A2A_SEND_BLOCKING = 70,
  A2A_RESUME_BLOCKING = 71,
  A2A_START_STREAM = 72,
  A2A_STOP_STREAM = 73,
  A2A_PULL_EVENTS = 74,
  REPLAY_SKILL = 75,
  REPLAY_TASK = 76,
}
